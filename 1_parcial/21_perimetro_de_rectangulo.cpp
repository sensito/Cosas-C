#include <stdio.h>
  main()
    {
      float a, b;
      printf("ingresa el valor del largo y ancho de tu rectangulo");
      scanf("%f,%f", &a, &b);
      printf("el valor del perimetro de tu rectangulo es:%f",(a+b)*2 );
    }
