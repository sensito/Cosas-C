#include <stdio.h>
  main()
    {
      float a;
      printf("ingresa el valor de uno de los lados de tu cuadrado");
      scanf("%f",&a );
      printf("el valor del perimetro de tucuadrado es:%f", a+a+a+a );
    }
