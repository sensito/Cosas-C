#include <stdio.h>
  main()
    {
      float b,a;
      printf("ingresa el valor de la base y altura de tu cuadrado");
      scanf("%f,%f",&b,&a);
      printf("el area de tu triangulo:%f",(b * a)/2 );
    }
