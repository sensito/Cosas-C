#include <stdio.h>


  int main(int argc, char const *argv[]) {
    int a, b, c;
    printf("ingresa el valor de los lados de tu triangulo\n");
    scanf("%d,%d,%d",a,b,c);
    if (a = b) {
    } else if (a = c) {
        printf("escaleno\n");
    } else {
        printf("izoceles\n");
    }
    return 0;
  }
