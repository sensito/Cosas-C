#include <stdio.h>
  main()
    {
      float r,pi;
      pi=3.1416;
      printf("ingresa el valor del radio de tu circulo");
      scanf("%f",&r);
      printf("el valor del perimetro de tu circulo es:%f", (2*r) * pi);
    }
