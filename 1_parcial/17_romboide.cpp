#include <stdio.h>
  main()
    {
      float a, b;
      printf("ingresa el valor de la longitud y altura de tu romboide");
      scanf("%f, %f", &a, &b);
      printf("el valor del perimetro de tu romboide es:%f", a+a+b+b);
    }
