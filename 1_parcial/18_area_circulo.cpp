#include <stdio.h>
  main()
      {
        float r,a;
        a = 3.1416;
        printf("ingresa el valor del radio de tu circulo");
        scanf("%f",&r);
        printf("El area de tu circulo es:%f",a*(r*r));
      }
